{
  "name": "confluence-pipeline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Polymarket data collection + backfill jobs for the Confluence project. Writes into Supabase (Postgres). Run via GitHub Actions cron.",
  "scripts": {
    "check-connection": "node src/checkConnection.js",
    "collect": "node src/collect.js",
    "backfill": "node src/backfill.js",
    "rank-wallets": "node src/rankWallets.js",
    "score-matches": "node src/scoreMatches.js",
    "paper-bets": "node src/paperBets.js",
    "test": "node src/polymarket.test.js && node src/collect.test.js && node src/rankWallets.test.js && node src/scoreMatches.test.js && node src/paperBets.test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "pg": "^8.13.1"
  }
}
